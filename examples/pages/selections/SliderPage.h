#ifndef SLIDERPAGE_H
#define SLIDERPAGE_H

#include "../BasePage.h"

class SliderPage : public BasePage {
    Q_OBJECT
  public:
    explicit SliderPage(QWidget* parent = nullptr);
};

#endif // SLIDERPAGE_H
