#ifndef NAVIGATIONVIEWPAGE_H
#define NAVIGATIONVIEWPAGE_H

#include "../BasePage.h"

class NavigationViewPage : public BasePage {
    Q_OBJECT

  public:
    explicit NavigationViewPage(QWidget* parent = nullptr);
};

#endif // NAVIGATIONVIEWPAGE_H
