#ifndef SCROLLAREAPAGE_H
#define SCROLLAREAPAGE_H

#include "../BasePage.h"

class ScrollAreaPage : public BasePage {
    Q_OBJECT

  public:
    explicit ScrollAreaPage(QWidget* parent = nullptr);
};

#endif // SCROLLAREAPAGE_H
