#ifndef DIALOGPAGE_H
#define DIALOGPAGE_H

#include "../BasePage.h"

class DialogPage : public BasePage {
    Q_OBJECT

  public:
    explicit DialogPage(QWidget* parent = nullptr);
};

#endif // DIALOGPAGE_H
