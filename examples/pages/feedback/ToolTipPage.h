#ifndef TOOLTIPPAGE_H
#define TOOLTIPPAGE_H

#include "../BasePage.h"

class ToolTipPage : public BasePage {
    Q_OBJECT

  public:
    explicit ToolTipPage(QWidget* parent = nullptr);
};

#endif // TOOLTIPPAGE_H
